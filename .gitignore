node_modules/
# Anchored to the repository root: an unanchored `build/` also matched
# `web/src/app/build/` and `web/src/components/build/`, which silently kept the
# entire Build Trial screen out of the repository.
/dist/
/build/
.next/
.env
.env.local
*.log
.DS_Store

# Scratch PR descriptions
PR*-description.md
